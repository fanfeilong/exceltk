namespace Exceltk.Reader.Package {
    /// <summary>
    /// A parsed semantic unit emitted by a streaming package parser.
    /// Not a file container — binary records and XML elements are packages.
    /// </summary>
    public interface IPackage {
    }
}
