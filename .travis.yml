# Travis CI is retired for this repo.
# Continuous integration now runs on GitHub Actions:
#   .github/workflows/ci.yml  (.NET 10)
#
# To run the same checks locally:
#   dotnet build src/Exceltk/Exceltk.csproj -c Release
#   (cd src && bash test.sh)
